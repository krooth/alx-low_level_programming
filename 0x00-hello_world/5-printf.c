#include <stdio.h>
#include <string.h>

/**
   * main - entry point
   * void - doesn't any
   *
   * Return: returns 0 when program finsishes excuting
   */

int main(void)
{
	printf("%s\n", "with proper grammar, but the outcome is a piece of art, ");
	return (0);
}
