#include "main.h"

/**
	* mul - returns the product of two ints
	* @a: first integer
	* @b: second integer
    *
	* Return: returns 0 when program finishes excution
	*/

int mul(int a, int b)
{
	int product = a * b;

	return (product);
}
