#include "main.h"


/**
   * main - entry point
   * void - no parameter for main
   *
   * Return: returns 0 when program finishes excution
   */

int main(void)
{
   char str[] = "_putchar";
   int i = 8;

   while (i < 8)
   {
      _putchar(str[i]);
      i++;

   }
   _putchar('\n');

	return (0);
}

