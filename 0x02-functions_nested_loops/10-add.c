#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and returns the result
 * @a: integer parmeter to be added
 * @b: integer parameter to be added
 *
 * Return: returns integer
 */
int add(int a, int b)
{
  int sum = a + b;
  
  return (sum);
}
